<hi>New Book</hi>

<%= form_with model: @book,url: book_path(@book.id),method: :patch do |f| %>

<h4>Taitle</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit'Create Book' %>

<% end %>